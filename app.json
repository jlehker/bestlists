{
  "scripts": {
    "dokku": {
      "predeploy": "(/usr/local/bin/supervisorctl -c /app/config/workers.conf scheduler stop || true) && /app/manage.py migrate --noinput && /app/manage.py collectstatic --noinput"
    }
  }
}